/** @type {import('next').NextConfig} */
const nextConfig = {
  // Config options go here. Turbopack is fine for local `next dev`; the
  // production build is pinned to Webpack via the "build" script
  // (next build --webpack) for Hostinger compatibility.
};

module.exports = nextConfig;
